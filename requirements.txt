--extra-index-url https://download.pytorch.org/whl/cu126

# CUDA wheel pins are intended for GPU training machines. If your environment
# uses a different CUDA runtime, install the matching PyTorch build first and
# then install the remaining dependencies.
torch==2.11.0+cu126
torchvision==0.26.0+cu126
torchaudio==2.11.0+cu126
transformers>=5.0.0
datasets>=2.18.0
accelerate>=1.0.0
peft>=0.19.0
trl>=1.8.0
bitsandbytes>=0.43.0
huggingface_hub>=0.23.0
sentencepiece>=0.1.99
safetensors>=0.4.3
pyyaml>=6.0.1
tqdm>=4.66.0
numpy>=1.24.0
pillow>=10.0.0
flask>=3.0.0
